# unixsock_example

Flutter example app for `unixsock`.

## Run

```bash
cd example
flutter run -d macos
```

You can also target other supported platforms, e.g.:

```bash
flutter run -d android
flutter run -d ios
flutter run -d linux
```
